# External Services
server:
    port: ${SERVER_PORT:8089}

# spring boot configurations
spring:
    application:
        name: vg.ms.capacity
    data:
        mongodb:
            uri: mongodb+srv://amirarbieto:n1230IGFtm6682Kj@artemiosolutions.pripjnm.mongodb.net/artemiosolutions?retryWrites=true&w=majority

# Microservice health meter
management:
  endpoint:
    health:
      show-details: always

# Empoit de actuator  

api:
  version: api/v1

# Swagger
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api/capacity
    
# External services
services:
    competency:
        url: ${COMPETENCY_SERVICE_URL:http://localhost:8084/common/api/v1/competencies}

